# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'LocalMusicFinder' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for LocalMusicFinder
  pod 'SnapKit', '~> 5.0'
  pod 'RxSwift', '~> 5.0'
  pod 'RxSwiftExt', '~> 5.0'
  pod 'Kingfisher', '~> 5.7'

  target 'LocalMusicFinderTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'LocalMusicFinderUITests' do
    # Pods for testing
  end

end
